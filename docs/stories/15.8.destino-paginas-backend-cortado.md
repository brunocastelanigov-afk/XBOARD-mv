# Story 15.8: Destino das paginas com backend cortado

Status: Develop

## Objetivo

Decidir e implementar o destino de `src/pages/dashboard.tsx`,
`src/pages/conquistas.tsx` e `src/pages/relatorios.tsx`, cujos backends
foram cortados do Epic 14, sem manter dado fabricado fingindo ser dado real.

## Contexto

Story 15.1 esta `Done` e fechou que a role CRM usa
`dashboard_role = "crm"` no Supabase do app. Epic 15 define que Dashboard,
Conquistas e Relatorios precisam de decisao explicita nesta story:
esconder, remover ou adiar com estado honesto. Relatorios nao pode reabrir
backend `admin_reports_*` nesta rodada; essa decisao ja foi descartada em
2026-08-13.

O Epic 14 removeu do escopo:

- Dashboard (P01): contrato `admin_dashboard_summary`, rollup e cache foram
  cortados; existe apenas view orfa herdada de migration anterior.
- Conquistas (P07): Story 14.13 inteira foi removida; nao existe contrato,
  rota ou migration `admin_achievements*`.
- Relatorios (P06): contratos `admin_reports_summary`,
  `admin_reports_users_series` e `admin_reports_assessments_series` foram
  cortados; Relatorios nao recebe backend novo nesta rodada.

Auditoria pre-draft em 2026-08-13 confirmou:

- `dashboard.tsx` exibe constantes fabricadas (`METRICS`, `METRIC_TILES`,
  `IDADE_DATA`, `OBJETIVOS_DATA`, `FREQUENCIA_DATA`,
  `RECENT_SUGGESTIONS`, `RECENT_EVALUATIONS`).
- `conquistas.tsx` exibe `MOCK_CONQUISTAS`, cria IDs `mock-${Date.now()}`
  e simula salvamento com `setTimeout`.
- `relatorios.tsx` exibe metricas, series e listas estaticas, incluindo
  `MOCK_SUGGESTIONS`, e permite alterar status localmente.
- `app-sidebar.tsx` ja esconde Relatorios via `group: null`; Dashboard e
  Conquistas continuam visiveis no grupo CRM.
- `main.tsx` protege Dashboard e Conquistas pelo grupo `crm`, mas
  `/crm/relatorios` segue fora do gate de grupo por decisao anterior,
  recebendo apenas o gate base de sessao/time.

## Escopo

1. Levantar via AskUserQuestion o destino de `/crm/dashboard`:
   esconder, remover ou adiar com estado honesto "indisponivel/em breve".
2. Levantar via AskUserQuestion o destino de `/crm/conquistas`:
   esconder, remover ou adiar com estado honesto.
3. Levantar via AskUserQuestion o destino de `/crm/relatorios`:
   esconder, remover ou adiar; nunca reabrir backend.
4. Implementar cada decisao em `app-sidebar.tsx`, `main.tsx` e/ou na
   propria pagina, sem deixar dado fabricado visivel.
5. Se alguma pagina for adiada, substituir conteudo fake por estado
   explicito sem metricas, listas, CRUD local ou timers de sucesso mockado.
6. Se alguma pagina for escondida, remover o item da sidebar e garantir que
   navegacao direta seja coerente com a decisao sem expor dados fabricados.
7. Se alguma pagina for removida, remover rota/item/conteudo somente com
   aprovacao explicita registrada, pois a regra do repo proibe remover
   conteudo sem perguntar antes.

## Fora de Escopo

- Integracao real de Dashboard, Conquistas ou Relatorios.
- Criar rota, migration, view/RPC, contrato `admin_*` ou backend novo.
- Reabrir `admin_reports_*` para Relatorios.
- Alterar as paginas das Stories 15.5, 15.6, 15.7 ou 15.9.
- Usar mock, fixture local, constante numerica ou array estatico como se
  fosse dado administrativo real.
- Alterar auth, split de ambiente, Supabase clients ou role gate fora do
  necessario para cumprir a decisao desta story.
- Remover pagina, rota, item de sidebar ou conteudo sem aprovacao explicita
  do usuario.

## Arquivos Provaveis

- `src/pages/dashboard.tsx`
- `src/pages/conquistas.tsx`
- `src/pages/relatorios.tsx`
- `src/components/composites/app-sidebar.tsx`
- `src/main.tsx`
- `tests/*.spec.ts`
- `docs/stories/15.8.destino-paginas-backend-cortado.md`

Leitura de contexto:

- `docs/stories/15.1.auth-cross-supabase-role-crm-scope.md`
- `treino-trinca-app/docs/epics/epic-15-admin-crm-dashboard-integration.md`
- `treino-trinca-app/docs/epics/epic-14-admin-crm-backend-expansion.md`

## Tarefas

1. [x] Ler Story 15.1, Epic 15 e Epic 14.
2. [x] Auditar `dashboard.tsx`, `conquistas.tsx`, `relatorios.tsx`,
       `app-sidebar.tsx` e `main.tsx`.
3. [x] Registrar decisao do usuario para `/crm/dashboard`.
4. [x] Registrar decisao do usuario para `/crm/conquistas`.
5. [x] Registrar decisao do usuario para `/crm/relatorios`.
6. [x] Registrar as tres decisoes na secao "Decisoes do Usuario".
7. [x] Implementar as decisoes por pagina.
8. [x] Remover dado fabricado visivel das tres paginas, conforme decisao.
9. [x] Ajustar/adicionar testes para sidebar, rota direta e ausencia de
       mocks nas paginas afetadas.
10. [x] Rodar verificacoes relevantes (`rg` de imports/rotas renderizadas, testes
        Playwright focados, typecheck/build conforme impacto).
11. [ ] Atualizar Dev Agent Record, File List, Change Log, QA Results e
        Status.

## Criterios de Aceite

- Cada uma das tres paginas tem decisao registrada e implementada.
- Nenhuma das tres paginas exibe `MOCK_*` ou constantes fabricadas como dado
  real apos esta story.
- Se esconder/remover for escolhido, o item sai da sidebar e a navegacao
  direta fica coerente com a decisao.
- Se adiar for escolhido, a UI mostra estado honesto sem metricas, listas
  mockadas, CRUD local, timers artificiais ou IDs fabricados.
- Relatorios nao reabre backend `admin_reports_*`.
- Nenhuma rota, contrato ou migration nova e criada no worker.
- O role gate existente de trafego/CRM nao sofre regressao.

## Testes

- Verificacao textual:
  `rg -n "from './pages/(dashboard|conquistas|relatorios)'|<DashboardPage|<ConquistasPage|<RelatoriosPage|title: \"Dashboard\"|title: \"Conquistas\"|title: \"Relatórios\"" src/main.tsx src/components/composites/app-sidebar.tsx`
  sem achados. Como a decisao foi "esconder", os arquivos de pagina nao
  sao deletados; a prova de ausencia de dado fabricado e que eles nao sao
  importados/renderizados por rota nem navegaveis pela sidebar.
- Playwright ou teste equivalente para:
  - comportamento escolhido de `/crm/dashboard`;
  - comportamento escolhido de `/crm/conquistas`;
  - comportamento escolhido de `/crm/relatorios`;
  - sidebar coerente para conta `dashboard_role = "crm"`;
  - navegacao direta coerente para conta CRM e conta de trafego;
  - regressao do gate de role existente.
- `npm run build` e/ou `npx tsc -b` conforme o impacto final.

## Dependencias

- Story 15.1 — Done: modelo CRM usa Supabase do app,
  `dashboard_role = "crm"`; Dashboard entra na lista CRM, enquanto
  Relatorios nao recebe integracao real.
- Story 15.3 — sidebar ja filtra por grupo.
- Story 15.4 — rota direta ja bloqueia grupos traffic/CRM.
- Epic 14 — backend cortado para Dashboard, Conquistas e Relatorios.

## Decisoes Bloqueantes

Resolvidas em 2026-08-13.

1. `/crm/dashboard`: esconder.
2. `/crm/conquistas`: esconder.
3. `/crm/relatorios`: esconder, sem reabrir backend.

Nota operacional: em 2026-08-13, a tentativa de usar a ferramenta
AskUserQuestion falhou porque ela nao esta disponivel no modo atual da
runtime. O usuario respondeu diretamente "Esconder", e a decisao foi
registrada nesta story antes da implementacao.

## Decisoes do Usuario

### Decisao 1 — `/crm/dashboard`

**Escolha:** esconder.

**Registro:** usuario respondeu "Esconder" em 2026-08-13, interpretado
para as tres paginas pendentes desta story.

**Implementacao:** item "Dashboard" removido da sidebar CRM. Navegacao
direta para `/crm/dashboard` passa pelo gate CRM e redireciona para
`/crm/usuarios`, sem renderizar `DashboardPage` nem exibir metricas/listas
fabricadas.

### Decisao 2 — `/crm/conquistas`

**Escolha:** esconder.

**Registro:** usuario respondeu "Esconder" em 2026-08-13, interpretado
para as tres paginas pendentes desta story.

**Implementacao:** item "Conquistas" removido da sidebar CRM. Navegacao
direta para `/crm/conquistas` passa pelo gate CRM e redireciona para
`/crm/usuarios`, sem renderizar `ConquistasPage` nem exibir
`MOCK_CONQUISTAS`.

### Decisao 3 — `/crm/relatorios`

**Escolha:** esconder.

**Registro:** usuario respondeu "Esconder" em 2026-08-13, interpretado
para as tres paginas pendentes desta story. Backend `admin_reports_*`
permanece fechado.

**Implementacao:** Relatorios ja nao aparecia na sidebar e permanece sem
item navegavel. A rota direta `/crm/relatorios` agora fica dentro do gate
CRM e redireciona para `/crm/usuarios` para contas CRM; contas de trafego
sao redirecionadas pelo role gate para `/roi-campanhas`. `RelatoriosPage`
nao e renderizada.

## Dev Notes

- Preferir "adiar" quando a pagina deve continuar existindo sem backend:
  manter rota/sidebar conforme decidido, mas renderizar um estado claro de
  indisponibilidade sem numeros, nomes, listas ou acoes fake.
- Preferir "esconder" quando a pagina nao deve ser navegavel agora:
  remover item da sidebar e tratar a rota direta com redirecionamento ou
  estado coerente, de acordo com a decisao registrada.
- "Remover" exige aprovacao explicita porque apaga conteudo/rota/item da
  experiencia.
- Nao criar backend `admin_dashboard_*`, `admin_achievements*` ou
  `admin_reports_*`.
- Nao usar `empty=1`, `loading=1` ou `canEdit=false` para mascarar dados
  fabricados; se a pagina for adiada, o estado honesto deve ser o fluxo
  normal da pagina.
- Se Relatorios continuar como rota direta, rever o comentario em
  `main.tsx`: o comportamento atual deixa `/crm/relatorios` fora do gate
  de grupo e acessivel para trafego e CRM.

## Checklist Pre-Draft

- [x] Story 15.1 lida.
- [x] Epic 15 lido.
- [x] Epic 14 lido.
- [x] `src/pages/dashboard.tsx` lido.
- [x] `src/pages/conquistas.tsx` lido.
- [x] `src/pages/relatorios.tsx` lido.
- [x] `src/components/composites/app-sidebar.tsx` lido.
- [x] `src/main.tsx` lido.
- [x] Testes existentes listados.
- [x] Draft criado antes de implementacao.

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- Leitura do anexo de objetivo em
  `/Users/brunogovas/.codex/attachments/31842caf-1760-4f83-827a-bed4c0824c3f/pasted-text-1.txt`.
- Leitura de
  `treino-trinca-app/docs/stories/15.1.auth-cross-supabase-role-crm-scope.md`.
- Leitura de
  `treino-trinca-app/docs/epics/epic-15-admin-crm-dashboard-integration.md`.
- Leitura de
  `treino-trinca-app/docs/epics/epic-14-admin-crm-backend-expansion.md`.
- Auditoria de `src/pages/dashboard.tsx`, `src/pages/conquistas.tsx`,
  `src/pages/relatorios.tsx`, `src/components/composites/app-sidebar.tsx`
  e `src/main.tsx`.
- Tentativa de AskUserQuestion bloqueada pela runtime: ferramenta
  indisponivel no modo atual.

### Completion Notes

- Draft criado.
- Usuario respondeu "Esconder" para as paginas pendentes.
- Dashboard, Conquistas e Relatorios foram ocultadas da navegacao e da rota
  direta sem deletar os arquivos de pagina.

### File List

- `docs/stories/15.8.destino-paginas-backend-cortado.md`
- `src/components/composites/app-sidebar.tsx`
- `src/components/composites/protected-route.tsx`
- `src/main.tsx`
- `tests/role-gate-route-guard.spec.ts`
- `tests/role-gate-sidebar.spec.ts`

## Change Log

| Data | Versao | Descricao | Autor |
|---|---:|---|---|
| 2026-08-13 | 0.1 | Draft inicial da Story 15.8 com auditoria pre-draft e decisoes bloqueantes. | GPT-5 Codex |
| 2026-08-13 | 0.2 | Registrada decisao "Esconder" para Dashboard, Conquistas e Relatorios; sidebar/rotas ajustadas para nao renderizar paginas com backend cortado. | GPT-5 Codex |

## QA Results

Pendente. Review so pode iniciar apos decisoes registradas e implementacao.
