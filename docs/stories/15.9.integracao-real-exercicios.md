# Story 15.9: Integracao real — Exercicios

Status: Done

## Objetivo

Substituir os dados fabricados de `src/pages/exercicios.tsx` por leitura
e escrita reais usando os contratos ja entregues no Epic 14: leitura via
`admin_exercises_list` e mutacoes via `POST /admin/exercises`,
`PATCH /admin/exercises/:id` e `DELETE /admin/exercises/:id`. O `DELETE`
e inativacao (`is_active = false`), nunca remocao fisica.

## Contexto

Exercicios (P04) foi adicionado ao Epic 15 em 2026-08-13 por decisao do
usuario. O backend ja esta pronto desde a Story 14.8, sem necessidade de
reabrir o Worker: `admin_exercises_list` expoe filtros por
`grupo_muscular`, `is_active`, `search_name_prefix` e paginacao keyset; o
Worker expoe apenas mutacoes admin em `/admin/exercises`.

Stories 15.1 e 15.2 ja fecharam que chamadas CRM usam o Supabase do app
por ambiente (`supabaseCrm`) e `VITE_API_URL`. A pagina atual ainda usa
`MOCK_EXERCISES`, categorias fixas (`MUSCLE_GROUPS`) e estado local para
criar, editar e excluir itens, incluindo IDs fabricados. Esta story troca
esse fluxo por contratos reais sem criar rota, migration, tabela de
categoria ou dado fake.

## Escopo

1. Mapear lista e filtros de `exercicios.tsx` para
   `admin_exercises_list`:
   - busca por prefixo de nome (`search_name_prefix`);
   - filtro por `grupo_muscular`;
   - filtro `is_active`;
   - keyset quando a tela precisar carregar alem do primeiro lote.
2. Mapear mutacoes:
   - criar exercicio: `POST /admin/exercises`;
   - editar exercicio: `PATCH /admin/exercises/:id`;
   - inativar exercicio: `DELETE /admin/exercises/:id`.
3. Remover `MOCK_EXERCISES`, categorias fixas e IDs/estado local
   fabricados da pagina.
4. Reusar helper admin existente com `VITE_API_URL`,
   `supabaseCrm.auth.getSession()` e `Idempotency-Key` nas mutacoes.
5. Implementar estados reais de loading, erro, vazio, salvamento e
   inativacao sem fallback fake.
6. Derivar categorias exibidas dos valores reais de `grupo_muscular`
   retornados pela listagem carregada.
7. Na inativacao, exibir `affectedTemplates` quando o `DELETE` retornar
   esse campo; nao inventar "em uso por N templates" antes da resposta.
8. Confirmar que programas/templates continuam legiveis apos inativacao
   usando o comportamento garantido pela Story 14.8.

## Fora de Escopo

- Criar rota GET `/admin/exercises` no Worker.
- Criar/alterar contrato `admin_*`, migration, tabela de categorias,
  enum ou coluna nova.
- Remover fisicamente qualquer exercicio real.
- Reativar exercicio inativo, upload/storage de video, ou qualquer nova
  feature fora da criacao/edicao/inativacao existente.
- Usuarios/liberar usuario, protocolos, regras, banners, configuracoes,
  avaliacao, dashboard, conquistas, relatorios ou qualquer outra pagina.
- Inventar contagem de templates em uso quando o contrato ainda nao a
  expos antes do `DELETE`.

## Arquivos Provaveis

- `src/pages/exercicios.tsx`
- `src/lib/admin-crm-api.ts` (leitura/reuso; editar so se estritamente
  necessario)
- `tests/admin-crm-real-integration.spec.ts`
- `docs/stories/15.9.integracao-real-exercicios.md`

Leitura sem escrita:

- `treino-trinca-app/docs/stories/14.8.admin-exercises.md`
- `treino-trinca-app/worker/src/routes/admin/exercises.ts`

## Tarefas

- [x] Draft: ler Story 15.1, Story 15.2, Epic 15 Story 15.9, Story 14.8,
      `worker/src/routes/admin/exercises.ts`, `exercicios.tsx`,
      `supabase-crm.ts`, `docs/deploy.md` e testes existentes.
- [x] Implementar leitura real via `admin_exercises_list`.
- [x] Remover mocks/categorias fixas/IDs fabricados.
- [x] Implementar criacao, edicao e inativacao reais com
      `Idempotency-Key`.
- [x] Exibir loading/erro/vazio/salvando/inativando reais.
- [x] Exibir `affectedTemplates` retornado pelo DELETE sem inventar
      dados antes da resposta.
- [x] Cobrir Playwright: listagem RPC, filtros/categorias reais,
      create/patch/delete com `Idempotency-Key`, ausencia de mocks.
- [x] Validar build/lint/testes relevantes.
- [x] Atualizar Dev Agent Record, File List, QA Results e status.

## Criterios de Aceite

- Nenhum `MOCK_*` remanescente em `src/pages/exercicios.tsx`.
- Lista usa `admin_exercises_list`, nao estado local fabricado.
- Criacao, edicao e inativacao usam `/admin/exercises` real.
- Toda mutacao envia `Idempotency-Key` e token CRM (`supabaseCrm`).
- Categorias exibidas derivam de `grupo_muscular` real.
- Inativacao nao remove fisicamente exercicio e nao quebra programas ja
  gerados.
- `affectedTemplates` e exibido quando retornado pelo `DELETE`.
- Estados de loading/erro sao reais, sem fallback fake.

## Testes

- Playwright com rotas mockadas de rede real:
  - `admin_exercises_list` retorna linhas reais e a pagina renderiza a
    lista sem `MOCK_EXERCISES`;
  - categorias aparecem a partir de `grupo_muscular`;
  - `POST /admin/exercises`, `PATCH /admin/exercises/:id` e
    `DELETE /admin/exercises/:id` sao chamados com `Idempotency-Key`;
  - resposta de DELETE com `affectedTemplates` aparece na confirmacao.
- `npm run lint`.
- `npm run build`.
- Validacao manual/staging quando houver credenciais: lista/CRUD em
  `lcylofpnwlwaicewhsfl`; inativar exercicio referenciado e confirmar
  template/programa legivel.

## Dependencias

- Story 15.1 — Done: modelo de auth CRM direto no Supabase do app.
- Story 15.2 — Done: `supabaseCrm` e `VITE_API_URL`.
- Story 14.8 — Done: backend de exercicios admin.

## Decisoes Bloqueantes

Nenhuma decisao de produto bloqueia o develop. Se surgir necessidade de
novo contrato, migration, rota GET ou entidade de categorias, parar e
perguntar antes de implementar, porque isso contradiz o escopo do Epic
15.

## Dev Notes

- `DELETE /admin/exercises/:id` significa inativacao e retorna o
  exercicio com `isActive: false` e `affectedTemplates`.
- `PATCH /admin/exercises/:id` nao altera `slug` nem `is_active`.
- `admin_exercises_list` usa campos snake_case no RPC; as mutacoes do
  Worker usam payload camelCase.
- Programas ja gerados usam snapshot estatico de exercicios; inativar ou
  editar exercicio nao reescreve `workout_days.exercicios_snapshot`.
- Categorias sao valores livres de `grupo_muscular`; nao existe tabela
  propria nem enum.

## Checklist Pre-Draft

- [x] Story 15.1 lida.
- [x] Story 15.2 lida.
- [x] Epic 15 Story 15.9 lido.
- [x] Story 14.8 lida.
- [x] `worker/src/routes/admin/exercises.ts` lido.
- [x] `src/pages/exercicios.tsx` lido.
- [x] Helpers CRM/admin e testes existentes lidos.

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

`npm run lint` limpo. `npx tsc -b` limpo. `npx playwright test
tests/admin-crm-real-integration.spec.ts` limpo (4/4). `npm run build`
limpo; apenas warning existente de chunk Vite > 500 kB. `rg -n
"MOCK_|mock-|MUSCLE_GROUPS|PILL_OPTIONS|Excluir definitivamente|DELETE
FROM|remove" src/pages/exercicios.tsx tests/admin-crm-real-integration.spec.ts
docs/stories/15.9.integracao-real-exercicios.md` sem ocorrencias em
`src/pages/exercicios.tsx` ou teste; ocorrencias apenas no texto da
story descrevendo o que foi removido.
`node /private/tmp/story-15-9-exercises-readonly.mjs` (read-only staging)
PASS: `admin_exercises_list` em `lcylofpnwlwaicewhsfl` retornou 5 linhas
ativas, filtro por `grupo_muscular` retornou 5 linhas, busca por prefixo
retornou 5 linhas e `admin_protocol_templates_tree` retornou 6 templates.
`node /private/tmp/story-15-9-exercises-smoke.mjs` (staging CRUD aprovado)
PASS: criou conta CRM QA, autenticou, listou exercicios, criou o
exercicio QA `58303d42-fc30-4371-a982-1a564d3643fb`, editou o mesmo
exercicio, inativou via `DELETE /admin/exercises/:id`, recebeu
`isActive=false`, `affectedTemplatesCount=0`, confirmou a linha pelo RPC
com `p_is_active=false` e confirmou templates legiveis (6 rows) apos a
inativacao. `node /private/tmp/story-15-9-qa-exercise-check.mjs` PASS:
0 exercicios QA Story 15.9 ativos e 2 inativos (a primeira tentativa
falhou so na leitura final de templates, depois de ja ter inativado sua
propria linha QA).

### Completion Notes

- `src/pages/exercicios.tsx` agora le `admin_exercises_list` via
  `adminRpc`, com `p_is_active`, `p_search_name_prefix`,
  `p_grupo_muscular`, keyset (`p_after_nome`/`p_after_exercise_id`) e
  `p_limit`.
- Categorias da tela e do datalist do formulario sao derivadas dos
  valores reais de `grupo_muscular` retornados pelo RPC, sem
  `MUSCLE_GROUPS`/lista fixa.
- Criacao, edicao e inativacao usam `adminMutation` contra
  `/admin/exercises`, `/admin/exercises/:id`; o helper injeta token CRM
  de `supabaseCrm` e `Idempotency-Key`.
- Inativacao remove o item da lista ativa local, nao executa remocao
  fisica, e exibe `affectedTemplates` retornado pelo `DELETE` quando
  presente.
- Upload/storage de video foi mantido fora do escopo; formulario grava
  somente URL externa (`videoUrl`) aceita pelo contrato do Worker.
- Nao houve alteracao em Worker, migration, contrato `admin_*` ou tabela
  de categorias.
- Smoke real staging aprovado pelo usuario em 2026-08-13 e executado
  contra `lcylofpnwlwaicewhsfl`/`api3.desafiotreinotrinca.online`.
  Somente exercicios QA com prefixo `QA Story 15.9` foram criados e
  inativados; nenhum exercicio existente foi editado ou removido.

### File List

- `docs/stories/15.9.integracao-real-exercicios.md`
- `src/pages/exercicios.tsx`
- `tests/admin-crm-real-integration.spec.ts`

## QA Results

### Review — 2026-08-13

- PASS: `src/pages/exercicios.tsx` nao contem `MOCK_*`, `mock-`,
  `MUSCLE_GROUPS` nem `PILL_OPTIONS`.
- PASS: Playwright cobre `admin_exercises_list`, categorias derivadas de
  `grupo_muscular`, `POST`/`PATCH`/`DELETE /admin/exercises` e
  `Idempotency-Key` nas tres mutacoes.
- PASS: resposta de `DELETE` com `affectedTemplates` aparece na UI.
- PASS: nenhum arquivo de Worker, migration ou contrato foi criado ou
  alterado.
- PASS: `npm run lint`, `npx tsc -b`, `npx playwright test
  tests/admin-crm-real-integration.spec.ts` e `npm run build`.
- PASS: `git show --stat HEAD` confirma commit de develop limitado a
  `docs/stories/15.9.integracao-real-exercicios.md`,
  `src/pages/exercicios.tsx` e
  `tests/admin-crm-real-integration.spec.ts`.
- PASS: staging read-only em `lcylofpnwlwaicewhsfl` validou
  `admin_exercises_list` real com lista ativa, filtro por
  `grupo_muscular`, busca por prefixo e leitura de templates
  (`admin_protocol_templates_tree`) sem mutar dados.
- PASS: CRUD real em staging aprovado e executado:
  `POST /admin/exercises`, `PATCH /admin/exercises/:id` e
  `DELETE /admin/exercises/:id` retornaram 200, todos com
  `Idempotency-Key`; o DELETE retornou `isActive=false` e
  `affectedTemplatesCount=0`.
- PASS: `admin_exercises_list(p_is_active=false,
  p_search_name_prefix='QA Story 15.9 ...')` confirmou a linha QA
  inativada; checagem final confirmou 0 exercicios QA Story 15.9 ativos.
- PASS: `admin_protocol_templates_tree` continuou legivel apos a
  inativacao (6 templates), cobrindo o teste manual de template/programa
  legivel dentro do contrato disponivel.

QA Gate: PASS

## Change Log

| Date | Version | Description | Author |
|---|---|---|---|
| 2026-08-13 | 0.1 | Draft criado para integrar Exercicios aos contratos reais do Epic 14, sem novo backend/migration e com DELETE como inativacao. | @sm |
| 2026-08-13 | 0.2 | Develop concluido: pagina integrada a `admin_exercises_list` e `/admin/exercises`, mocks removidos, categorias reais, `affectedTemplates` exibido e Playwright cobrindo lista/CRUD com `Idempotency-Key`. Status: Review. | @dev |
| 2026-08-13 | 0.3 | Review automatizado concluido com lint/typecheck/build/Playwright/searches PASS; CRUD real em staging permanece pendente de aprovacao explicita por alterar dados reais. | @qa |
| 2026-08-13 | 0.4 | Review staging read-only PASS para `admin_exercises_list` e templates reais em `lcylofpnwlwaicewhsfl`; smoke CRUD real nao executado por falta de aprovacao explicita para mutar staging. | @qa |
| 2026-08-13 | 0.5 | Review staging CRUD aprovado e PASS: exercicio QA criado, editado e inativado; `Idempotency-Key`, `isActive=false`, ausencia de QA ativo e templates legiveis confirmados. Status: Done. | @qa |
